import { } from './actions';

export const reducer = (state, actions) => {

  switch (actions.type) {
    // case GET_NUMBER:
    //   return {
    //     ...state,
    //     params: {
    //       ...state.params,
    //       [actions.id]: actions.number,
    //     },
    //   };

    default:
      return state;
  };
};
